appId: ${APP_ID}
---
- launchApp:
    clearState: true
    arguments:
      sentryAuthToken: ${SENTRY_AUTH_TOKEN}


- tapOn: "Capture Message"

- extendedWaitUntil:
    visible:
      id: "eventId"
    timeout: 60_000

- copyTextFrom:
    id: "eventId"
- assertTrue: ${maestro.copiedText}
