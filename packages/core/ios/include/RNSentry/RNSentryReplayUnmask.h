// The namespaced spelling (<RNSentry/RNSentryReplayUnmask.h>) of
// `ios/Replay/RNSentryReplayUnmask.h`, matching how CocoaPods header maps expose it. Swift Package
// Manager has no header maps, so public headers are mirrored here — the directory is the target's
// `publicHeadersPath`. See `Package.swift`.
#import "../../Replay/RNSentryReplayUnmask.h"
