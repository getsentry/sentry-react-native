// The namespaced spelling (<RNSentry/RNSentrySDK.h>) of `ios/RNSentrySDK.h`, matching how
// CocoaPods header maps expose it. Swift Package Manager has no header maps, so
// public headers are mirrored here — the directory is the target's
// `publicHeadersPath`. See `Package.swift`.
#import "../../RNSentrySDK.h"
