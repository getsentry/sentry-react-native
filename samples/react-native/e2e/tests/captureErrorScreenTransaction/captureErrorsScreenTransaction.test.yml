appId: io.sentry.reactnative.sample
---
- launchApp:
    # We expect cold start
    clearState: true
    stopApp: true
    arguments:
      isE2ETest: true

# The app should launch on the ErrorsScreen (first tab)
# Just wait for it to be ready
- assertVisible: "Capture message"
